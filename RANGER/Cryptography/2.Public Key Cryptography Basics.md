

In reality, you need more cryptography to verify that the person you’re talking to is who they say they are. This is achieved using digital signatures and certificates.

---
### **RSA :**

RSA is a method for encrypting data using two keys: one to lock (encrypt) and another to unlock (decrypt) it, relying on the difficulty of factoring large number. It is a public-key encryption algorithm that enables secure data transmission over insecure channels.

The public key is known to all correspondents and is used for encryption, while the private key is protected and used for decryption, as shown in the figure below.

![Alice encrypts the message with Bob's public key and Bob decrypts it with his private key.](https://tryhackme-images.s3.amazonaws.com/user-uploads/5f04259cf9bf5b57aed2c476/room-content/5f04259cf9bf5b57aed2c476-1725294065881.svg)

|Symbol|Meaning|
|---|---|
|**p, q**|Large prime numbers|
|**n**|p × q|
|**ϕ(n)**|Euler’s Totient = (p−1)(q−1)|
|**e**|Public exponent|
|**d**|Private exponent|
|**m**|Plaintext (message)|
|**c**|Ciphertext|
#### **Key Generation Steps :**

1 . Bob chooses two prime numbers: _p_ = 157 and _q_ = 199. He calculates _n_ = _p_ × _q_ = 31243.

2 . With _ϕ_(_n_) = _n_ − _p_ − _q_ + 1 = 31243 − 157 − 199 + 1 = 30888, Bob selects _e_ = 163 such that _e_ is relatively prime to _ϕ_(_n_); moreover, he selects _d_ = 379, where _e_ × _d_ = 1 mod _ϕ_(_n_), i.e., _e_ × _d_ = 163 × 379 = 61777 and 61777 mod 30888 = 1. The public key is (_n_,_e_), i.e., (31243,163) and the private key is $(n,d), i.e., (31243,379).

3 . Let’s say that the value they want to encrypt is _x_ = 13, then Alice would calculate and send _y_ = _x__e_ mod _n_ = 13163 mod 31243 = 16341.

4 . Bob will decrypt the received value by calculating _x_ = _y__d_ mod _n_ = 16341379 mod 31243 = 13. This way, Bob recovers the value that Alice sent.

## 1. Choose Two Prime Numbers

Select two prime numbers:

`p = 157 q = 199`
## 2. Compute n

Multiply the primes:

`n = p × q n = 157 × 199 n = 31243`

`n` is part of both the public and private keys.

---

## 3. Compute Euler’s Totient ϕ(n)

Using the formula:

`ϕ(n) = n − p − q + 1`

Substitute values:

`ϕ(n) = 31243 − 157 − 199 + 1 ϕ(n) = 30888`

---

## 4. Choose Public Exponent e

Choose `e` such that:

- `1 < e < ϕ(n)`
    
- `gcd(e, ϕ(n)) = 1`
    

Chosen value:

`e = 163`

---

## 5. Compute Private Exponent d

Find `d` such that:

`e × d ≡ 1 (mod ϕ(n))`

Calculation:

`163 × 379 = 61777 61777 mod 30888 = 1`

Therefore:

`d = 379`



_n_ − _p_ − _q_ + 1