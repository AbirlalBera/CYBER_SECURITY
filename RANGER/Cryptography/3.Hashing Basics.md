
A **hash value** is a fixed-size string or characters that is computed by a hash function. A **hash function** takes an input of an arbitrary size and returns an output of fixed length, i.e., a hash value.


## Real-world example (Linux)

To verify a download:

```
sha256sum file.iso
```

Compare the output with the official SHA-256 hash published on the website.

---
# ==`Hash Functions`==

### What is a Hash Function?

A **hash function** is a one-way mathematical function that takes input data of any size and produces a fixed-size output called a **hash value**. It is designed so that the same input always produces the same output, small changes in input cause large changes in output, and it is computationally impractical to reverse the process or find two different inputs with the same hash.

### Why is Hashing Important?

**Hashing is important because it ensures data integrity and protects sensitive information without storing the original data.**

 **Integrity verification:** Hashing allows you to check whether data (files, downloads, messages) has been altered. If the hash changes, the data has changed.

**Password security:** Systems store **hashed passwords** instead of plaintext passwords, reducing damage if a database is compromised.

**Authentication:** Hashes are used to verify identities without revealing secrets.

**Efficiency:** Large data can be represented by a small fixed-size hash, making comparisons fast.

**Tamper detection:** Even a one-bit change produces a completely different hash, making unauthorized changes easy to detect.

### What’s a Hash Collision?

A **hash collision** occurs when **two different inputs produce the same hash output**.

### Why collisions are dangerous

If attackers can **intentionally create collisions**, they can:

- Bypass file integrity checks
- Forge digital signatures
- Replace trusted files with malicious ones

#### ==`Usefull Commands`== :

Shows the contents of `file2.txt` in hexadecimal and ASCII format.
```
hexdump -C  filename     
```

Calculates the **MD5 hash** of all `.txt` files in the directory.
```
md5sum *.txt
```

Calculates the **SHA-1 hash** of all `.txt` files.
```
sha1sum *.txt
```

Calculates the **SHA-256 hash** of all `.txt` files.
```
sha256sum *.txt
```

---
### ==`rockyou.txt`== password list on Kali Linux.


---
# Using Hashing for Secure Password Storage

Hashing is a secure method for storing passwords without keeping the actual plaintext passwords. It helps protect user credentials even if a database is compromised.

## **How Hashing Works for Passwords**

1. Instead of storing the password itself, store its **hash value**.
2. When a user logs in, hash the entered password and compare it with the stored hash.
3. If the hashes match → grant access.  
This way, the system never needs to know or store the actual password.


## **Problems with Simple Hashing**

- If two users have the **same password**, the hash will be identical for both.
- An attacker cracking one hash gains access to **multiple accounts**.
- **Rainbow tables** can be used to reverse hashes to plaintext passwords.

A **==`Rainbow Table`==** is a lookup table of hashes to plaintexts, so you can quickly find out what password a user had just from the hash.A rainbow table trades the time to crack a hash for hard disk space, but it takes time to create.

![[Pasted image 20260130233353.png]]

## **Protecting Against Rainbow Tables: Salting**

A **salt** is a **random value** added to a password before hashing.Each user should have a **unique salt**.The salt ensures that even if two users have the same password, their hashes are **different**.Salts **do not need to be secret**; they are stored with the hash in the database.

**Example workflow**:
```
1. Password: `AL4RMc10k`
  
2. Salt: `Y4UV*^(=go_!`
  
3. Concatenate: `AL4RMc10kY4UV*^(=go_!`

4. Hash the combined string using a secure algorithm (Bcrypt, Scrypt, Argon2, PBKDF2)

5. Store the hash and the salt in the database.
```

## **Why Not Use Encryption Instead?**

**Encrypting passwords is not ideal because** :
```
Encryption is reversible.
You must store the encryption key.
If the key is compromised, all passwords can be decrypted.
Hashing avoids this problem because it is one-way.
```

---
# Recognising Password Hashes

**Recognising Hash Types** : Before cracking a hash, you must determine **what type of hash it is**.
### Automated Tools

- Tools like **hashID** can guess hash types
- They are **not always reliable**
- Some hashes (e.g., MD5, NTLM) look identical

Example:

- Hash found in a **web application database** → likely **MD5**
- Hash dumped from **Windows system** → likely **NTLM**

### Where Linux Stores Password Hashes

Stored in :
```
/etc/shadow
```

Only readable by **root**.Older systems used `/etc/passwd` (insecure)

The ==`shadow`== file contains the password information. Each line contains nine fields, separated by colons (`:`). The first two fields are the login name and the encrypted password. More information about the other fields can be found by executing `man 5 shadow` on a Linux system.

Structure of ==`/etc/shadow`==
```
username:encrypted_password:last_change:min:max:warn:inactive:expire:reserved
```

### ==`Linux Hash Format`==

The encrypted password field follows this format:

```
$prefix$options$salt$hash
```

**Where:**
- **prefix** → hashing algorithm used
- **options** → parameters for the algorithm
- **salt** → random value
- **hash** → resulting hash value

## **Common Linux Hash Prefixes**

| Prefix                         | Algorithm                                            |
| ------------------------------ | ---------------------------------------------------- |
| `$y$`                          | yescrypt (modern, strongest, default on new systems) |
| `$gy$`                         | gost‑yescrypt                                        |
| `$7$`                          | scrypt                                               |
| `$2b$`, `$2y$`, `$2a$`, `$2x$` | bcrypt                                               |
| `$6$`                          | sha512crypt                                          |
| `$md5`                         | SunMD5                                               |
| `$1$`                          | md5crypt                                             |

## **Modern Linux Example**

```
strategos:$y$j9T$76UzfgEM5PnymhQ7TlJey1$/OOSg64dhfF.TigVPdzqiFang6uZA4QA1pzzegKdVm4:19965:0:99999:7:::
```

### Breakdown:

- **Username:** `strategos`
- **Algorithm:** `$y$` → yescrypt
- **Options:** `j9T`
- **Salt:** `76UzfgEM5PnymhQ7TlJey1`
- **Hash:** `/OOSg64dhfF.TigVPdzqiFang6uZA4QA1pzzegKdVm4`
This prefix makes Linux hashes **easy to recognise**.

## **==`MS Windows Password Hashes`==**

### Windows Hashing Algorithm

- Uses **NTLM**
- NTLM is based on **MD4**
- Visually similar to **MD4 / MD5**

### Where Windows Stores Password Hashes

- Stored in the **SAM (Security Accounts Manager)**
- Normally protected by the OS
- Can be extracted using tools like:
- ==`mimikatz`==

### Types of Windows Hashes

- **NT hash** (main one used today)
- **LM hash** (older, weaker, often disabled)

---
## **Cracking Passwords with GPUs**

### Why GPUs are Used

- GPUs have **thousands of cores**
- They are extremely fast at performing the mathematical operations used in hash functions
- This allows attackers to test **millions or billions of passwords per second**

### GPU-Resistant Hashes

Some hashing algorithms are intentionally designed to be slow and memory‑intensive:

- **bcrypt**
- **scrypt**
- **Argon2**

These algorithms:
- Reduce GPU speed advantages
- Make large‑scale cracking more expensive and time‑consuming

Hashcat uses the following basic syntax: 

```
hashcat -m <hash_type> -a <attack_mode> hashfile wordlist
```

```
hashcat -m 3200 -a 0 hash.txt /usr/share/wordlists/rockyou.txt
```

find the ==`-m`== value from https://hashcat.net/wiki/doku.php?id=example_hashes

---
## **Tools Used for Hash Cracking**

Common tools include:

- **Hashcat**
- **John the Ripper**

These tools automate:

- Password guessing
- Salting
- Hash computation
- Comparison against the target hash

---
# ==`Hashing for Integrity Checking`==

Hashing isn’t just for passwords — it’s also widely used to **ensure files haven’t been modified**.

## **1️⃣ File Integrity Checking**

- Hashing a file generates a **fixed-length hash value** (checksum) that uniquely represents the file content.If **even a single bit** in the file changes, the hash will change dramatically.

This allows verification that:

Files are **unchanged**
Downloads are **identical to the source**
Duplicate files can be detected

### **Example: Fedora ISO Verification**

```
sha256sum Fedora-Workstation-Live-x86_64-40-1.14.iso
```

Compare the output hash with the official hash from a signed CHECKSUM file. If they match → ✅ File is authentic.

## **2️⃣ HMACs (Keyed-Hash Message Authentication Codes)**

HMACs combine a **hash function** with a **secret key** to verify:

1. **Integrity:** Message hasn’t been modified
2. **Authenticity:** Message was created by someone with the secret key

### **How HMAC Works (Step-by-Step)**

1. **Pad the secret key** to the hash function’s block size.
2. XOR the key with a constant (`ipad` for inner pad, `opad` for outer pad).
3. **Hash the message** combined with the XORed inner key:

	`H((K ⊕ ipad) || M)`
4. **Hash again** using the outer key:

    `H((K ⊕ opad) || H((K ⊕ ipad) || M))`
5. Result → **HMAC value** (fixed-size string)

![[Pasted image 20260131005941.png]]
### **HMAC Formula**

```
HMAC(K,M)=H((K⊕opad)∣∣H((K⊕ipad)∣∣M))
```
- `H` → Hash function (e.g., SHA-256)
- `K` → Secret key
- `M` → Message
- `⊕` → XOR operation
- `||` → Concatenation
- `ipad` and `opad` → Constants for inner and outer padding